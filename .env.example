# =====================================================
# CONSTRUBÃO - Variáveis de Ambiente
# Copie este arquivo para .env e configure os valores
# =====================================================

# Ambiente (development, production)
APP_ENV=development
APP_DEBUG=true

# Banco de Dados
DB_HOST=localhost
DB_NAME=construbao
DB_USER=root
DB_PASS=
DB_CHARSET=utf8mb4

# Site
# Use "auto" para detecção automática do domínio ou especifique a URL completa
# Exemplos:
#   SITE_URL=auto                           (detecta automaticamente)
#   SITE_URL=http://localhost/construbao    (desenvolvimento local)
#   SITE_URL=https://construbao.com.br      (produção - domínio próprio)
#   SITE_URL=https://cliente.com/construbao (produção - subdiretório)
SITE_URL=auto
SITE_NAME=Construbão
SITE_DESCRIPTION=Venda de Poste Padrão e Locação de Equipamentos para Construção

# WhatsApp
WHATSAPP_NUMBER=5516997007775
WHATSAPP_MESSAGE=Olá! Vim pelo site e gostaria de um orçamento.

# Upload
MAX_UPLOAD_SIZE=2097152
